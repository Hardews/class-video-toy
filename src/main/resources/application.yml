server:
  port: 8090

spring:
  main:
    allow-bean-definition-overriding: true

  datasource:
    username: root
    password: ________
    url: jdbc:mysql://________/bilibili?autoReconnect=true&amp&failOverReadOnly=false&useSSL=false&serverTimezone=GMT%2B8
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto